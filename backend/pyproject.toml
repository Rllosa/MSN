[tool.ruff]
line-length = 88
target-version = "py311"
# alembic/ contains migration files managed by the alembic tool; exclude from lint
exclude = ["alembic/"]

[tool.ruff.lint]
# E/W: pycodestyle, F: pyflakes, I: isort, N: pep8-naming, UP: pyupgrade
select = ["E", "F", "I", "N", "W", "UP"]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["app"]

[tool.ruff.lint.per-file-ignores]
# TODO(SOLO-102): resolve with ruff --fix once ruff is installed in local dev env
"tests/test_migrations.py" = ["I001"]
"tests/test_schema_contracts.py" = ["I001"]

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
# Add backend/ to sys.path so `import app.main` resolves when running from backend/
pythonpath = ["."]

[tool.coverage.run]
source = ["app"]
omit = ["tests/*"]

[tool.coverage.report]
show_missing = true
skip_covered = false
